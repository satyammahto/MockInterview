<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>PrepSense — AI Mock Interview Platform</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;1,300&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #080B14;
    --surface: #0E1220;
    --surface2: #141926;
    --border: #1E2535;
    --accent: #4EFFA3;
    --accent2: #7B61FF;
    --accent3: #FF6B6B;
    --accent4: #FFD166;
    --text: #E8EDF5;
    --text2: #8892A4;
    --text3: #4A5568;
    --card: #111827;
    --glow: rgba(78, 255, 163, 0.15);
    --glow2: rgba(123, 97, 255, 0.15);
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--bg);
    color: var(--text);
    overflow-x: hidden;
  }

  h1, h2, h3, h4, h5 { font-family: 'Syne', sans-serif; }

  /* NAV */
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    display: flex; align-items: center; justify-content: space-between;
    padding: 16px 48px;
    background: rgba(8, 11, 20, 0.85);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
  }

  .logo {
    font-family: 'Syne', sans-serif;
    font-size: 22px; font-weight: 800;
    color: var(--text);
    display: flex; align-items: center; gap: 10px;
  }

  .logo-dot {
    width: 10px; height: 10px; border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 12px var(--accent);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.6; transform: scale(0.85); }
  }

  .nav-links { display: flex; gap: 32px; }
  .nav-links a {
    color: var(--text2); font-size: 14px; text-decoration: none;
    transition: color 0.2s;
    cursor: pointer;
  }
  .nav-links a:hover, .nav-links a.active { color: var(--text); }

  .nav-cta {
    background: var(--accent); color: #000;
    padding: 10px 24px; border-radius: 8px;
    font-family: 'Syne', sans-serif; font-weight: 700; font-size: 14px;
    cursor: pointer; border: none; transition: all 0.2s;
  }
  .nav-cta:hover { transform: translateY(-1px); box-shadow: 0 8px 24px rgba(78,255,163,0.3); }

  /* PAGE SYSTEM */
  .page { display: none; min-height: 100vh; padding-top: 72px; }
  .page.active { display: block; }

  /* ============ LANDING PAGE ============ */
  .hero {
    min-height: calc(100vh - 72px);
    display: flex; flex-direction: column; align-items: center; justify-content: center;
    text-align: center; padding: 80px 24px;
    position: relative; overflow: hidden;
  }

  .hero-bg {
    position: absolute; inset: 0; pointer-events: none;
  }

  .hero-blob {
    position: absolute; border-radius: 50%; filter: blur(120px);
    animation: float 8s ease-in-out infinite;
  }

  .blob1 {
    width: 600px; height: 600px;
    background: radial-gradient(circle, rgba(78,255,163,0.08) 0%, transparent 70%);
    top: -200px; left: -100px;
  }

  .blob2 {
    width: 500px; height: 500px;
    background: radial-gradient(circle, rgba(123,97,255,0.1) 0%, transparent 70%);
    bottom: -100px; right: -100px;
    animation-delay: -4s;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-30px) scale(1.05); }
  }

  .hero-badge {
    display: inline-flex; align-items: center; gap: 8px;
    background: rgba(78,255,163,0.08); border: 1px solid rgba(78,255,163,0.2);
    border-radius: 100px; padding: 6px 16px; margin-bottom: 28px;
    font-size: 13px; color: var(--accent); font-weight: 500;
  }

  .hero-badge-dot {
    width: 6px; height: 6px; border-radius: 50%; background: var(--accent);
    animation: pulse 1.5s infinite;
  }

  .hero-title {
    font-size: clamp(48px, 7vw, 88px);
    font-weight: 800; line-height: 1.0;
    letter-spacing: -2px; margin-bottom: 24px;
    max-width: 900px;
  }

  .hero-title .highlight {
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-sub {
    font-size: 18px; color: var(--text2); max-width: 560px;
    line-height: 1.7; margin-bottom: 48px;
    font-weight: 300;
  }

  .hero-ctas { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }

  .btn-primary {
    background: var(--accent); color: #000;
    padding: 16px 36px; border-radius: 12px;
    font-family: 'Syne', sans-serif; font-weight: 700; font-size: 16px;
    cursor: pointer; border: none; transition: all 0.25s;
    display: flex; align-items: center; gap: 10px;
  }
  .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 16px 40px rgba(78,255,163,0.35); }

  .btn-secondary {
    background: transparent; color: var(--text);
    padding: 16px 36px; border-radius: 12px;
    font-family: 'Syne', sans-serif; font-weight: 600; font-size: 16px;
    cursor: pointer; border: 1px solid var(--border); transition: all 0.25s;
    display: flex; align-items: center; gap: 10px;
  }
  .btn-secondary:hover { border-color: var(--text2); background: var(--surface); }

  .hero-stats {
    display: flex; gap: 48px; margin-top: 80px;
    justify-content: center; flex-wrap: wrap;
  }

  .hero-stat { text-align: center; }
  .hero-stat-num {
    font-family: 'Syne', sans-serif; font-size: 36px; font-weight: 800;
    color: var(--text); line-height: 1;
  }
  .hero-stat-num span { color: var(--accent); }
  .hero-stat-label { font-size: 13px; color: var(--text3); margin-top: 4px; }

  /* Features Section */
  .section { padding: 100px 48px; }
  .section-label {
    font-size: 12px; font-weight: 600; letter-spacing: 3px;
    color: var(--accent); text-transform: uppercase; margin-bottom: 16px;
  }
  .section-title {
    font-size: clamp(32px, 4vw, 52px); font-weight: 800;
    line-height: 1.1; letter-spacing: -1px; margin-bottom: 64px;
    max-width: 600px;
  }

  .features-grid {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 24px; max-width: 1200px; margin: 0 auto;
  }

  .feature-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 20px; padding: 32px;
    transition: all 0.3s; position: relative; overflow: hidden;
  }

  .feature-card::before {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(135deg, var(--glow), transparent);
    opacity: 0; transition: opacity 0.3s;
  }

  .feature-card:hover { border-color: rgba(78,255,163,0.3); transform: translateY(-4px); }
  .feature-card:hover::before { opacity: 1; }

  .feature-icon {
    width: 52px; height: 52px; border-radius: 14px;
    display: flex; align-items: center; justify-content: center;
    font-size: 24px; margin-bottom: 20px;
  }

  .feature-card h3 { font-size: 18px; font-weight: 700; margin-bottom: 10px; }
  .feature-card p { font-size: 14px; color: var(--text2); line-height: 1.7; }

  /* ============ SETUP PAGE ============ */
  .setup-container {
    max-width: 780px; margin: 0 auto; padding: 60px 24px;
  }

  .setup-header { margin-bottom: 48px; }
  .setup-step-indicator {
    display: flex; align-items: center; gap: 0; margin-bottom: 40px;
  }

  .step-item {
    display: flex; align-items: center; gap: 10px; cursor: pointer;
  }

  .step-circle {
    width: 36px; height: 36px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-family: 'Syne', sans-serif; font-size: 14px; font-weight: 700;
    border: 2px solid var(--border); color: var(--text3);
    transition: all 0.3s;
  }

  .step-item.active .step-circle {
    background: var(--accent); border-color: var(--accent); color: #000;
  }

  .step-item.done .step-circle {
    background: rgba(78,255,163,0.15); border-color: var(--accent); color: var(--accent);
  }

  .step-label { font-size: 13px; color: var(--text3); font-weight: 500; }
  .step-item.active .step-label { color: var(--text); }

  .step-line {
    flex: 1; height: 1px; background: var(--border); margin: 0 8px;
    min-width: 40px;
  }

  /* Upload Zone */
  .upload-zone {
    border: 2px dashed var(--border); border-radius: 20px;
    padding: 60px 40px; text-align: center;
    cursor: pointer; transition: all 0.3s; position: relative;
    background: var(--surface);
  }

  .upload-zone:hover, .upload-zone.dragover {
    border-color: var(--accent);
    background: rgba(78,255,163,0.04);
  }

  .upload-icon {
    width: 72px; height: 72px; border-radius: 18px;
    background: rgba(78,255,163,0.1); border: 1px solid rgba(78,255,163,0.2);
    display: flex; align-items: center; justify-content: center;
    font-size: 32px; margin: 0 auto 20px;
  }

  .upload-zone h3 { font-size: 20px; margin-bottom: 8px; }
  .upload-zone p { color: var(--text2); font-size: 14px; }
  .upload-zone .upload-hint { font-size: 12px; color: var(--text3); margin-top: 12px; }

  /* Form Elements */
  .form-group { margin-bottom: 28px; }
  .form-label {
    display: block; font-size: 13px; font-weight: 600;
    color: var(--text2); margin-bottom: 10px; letter-spacing: 0.5px;
    text-transform: uppercase;
  }

  .form-input, .form-textarea, .form-select {
    width: 100%; background: var(--surface);
    border: 1px solid var(--border); border-radius: 12px;
    padding: 14px 18px; color: var(--text); font-size: 15px;
    font-family: 'DM Sans', sans-serif;
    transition: border-color 0.2s; outline: none;
  }

  .form-input:focus, .form-textarea:focus, .form-select:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px rgba(78,255,163,0.08);
  }

  .form-textarea { min-height: 120px; resize: vertical; }
  .form-select option { background: var(--surface); }

  .tag-selector { display: flex; flex-wrap: wrap; gap: 10px; }

  .tag {
    padding: 8px 18px; border-radius: 100px;
    border: 1px solid var(--border); font-size: 13px;
    cursor: pointer; transition: all 0.2s; color: var(--text2);
    font-weight: 500;
  }

  .tag:hover { border-color: var(--accent2); color: var(--text); }
  .tag.selected { background: rgba(123,97,255,0.15); border-color: var(--accent2); color: var(--accent2); }

  .setup-actions {
    display: flex; justify-content: space-between; align-items: center;
    margin-top: 48px;
  }

  /* Difficulty Slider */
  .difficulty-track {
    display: flex; gap: 8px;
  }

  .diff-btn {
    flex: 1; padding: 12px; border-radius: 10px;
    border: 1px solid var(--border); text-align: center;
    cursor: pointer; transition: all 0.2s; font-size: 13px; font-weight: 500;
    color: var(--text2);
  }

  .diff-btn.easy.selected { background: rgba(78,255,163,0.1); border-color: var(--accent); color: var(--accent); }
  .diff-btn.medium.selected { background: rgba(255,209,102,0.1); border-color: var(--accent4); color: var(--accent4); }
  .diff-btn.hard.selected { background: rgba(255,107,107,0.1); border-color: var(--accent3); color: var(--accent3); }

  /* ============ INTERVIEW PAGE ============ */
  .interview-layout {
    display: grid; grid-template-columns: 1fr 380px;
    min-height: calc(100vh - 72px);
    gap: 0;
  }

  .interview-main {
    padding: 48px; display: flex; flex-direction: column;
    border-right: 1px solid var(--border);
  }

  .interview-sidebar {
    padding: 32px; background: var(--surface);
    display: flex; flex-direction: column; gap: 24px;
  }

  .question-counter {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 32px;
  }

  .q-badge {
    font-family: 'Syne', sans-serif; font-size: 13px; font-weight: 700;
    color: var(--accent); background: rgba(78,255,163,0.1);
    border: 1px solid rgba(78,255,163,0.2);
    padding: 6px 14px; border-radius: 100px;
  }

  .q-timer {
    display: flex; align-items: center; gap: 8px;
    font-family: 'Syne', sans-serif; font-size: 28px; font-weight: 800;
    color: var(--text);
  }

  .q-timer-circle {
    width: 48px; height: 48px;
    border-radius: 50%;
    border: 3px solid var(--accent);
    display: flex; align-items: center; justify-content: center;
    font-size: 16px; font-weight: 700;
    position: relative;
  }

  .question-box {
    background: var(--surface); border: 1px solid var(--border);
    border-radius: 24px; padding: 40px; flex: 1;
    position: relative; overflow: hidden;
  }

  .question-box::before {
    content: '"';
    position: absolute; top: -20px; left: 20px;
    font-size: 160px; color: rgba(78,255,163,0.04);
    font-family: 'Syne', sans-serif; font-weight: 800;
    line-height: 1;
  }

  .question-type-tag {
    display: inline-block;
    padding: 4px 12px; border-radius: 6px; font-size: 11px;
    font-weight: 700; letter-spacing: 1px; text-transform: uppercase;
    background: rgba(123,97,255,0.15); color: var(--accent2);
    border: 1px solid rgba(123,97,255,0.2); margin-bottom: 20px;
  }

  .question-text {
    font-size: 26px; font-weight: 700; line-height: 1.4;
    letter-spacing: -0.5px;
  }

  /* AI Voice Visualizer */
  .voice-visualizer {
    display: flex; align-items: center; justify-content: center;
    gap: 4px; height: 60px; margin: 32px 0;
  }

  .voice-bar {
    width: 4px; border-radius: 2px; background: var(--accent);
    animation: voiceAnim 0.8s ease-in-out infinite;
    opacity: 0.6;
  }

  .voice-bar:nth-child(1) { height: 20px; animation-delay: 0s; }
  .voice-bar:nth-child(2) { height: 35px; animation-delay: 0.1s; }
  .voice-bar:nth-child(3) { height: 50px; animation-delay: 0.2s; }
  .voice-bar:nth-child(4) { height: 40px; animation-delay: 0.15s; }
  .voice-bar:nth-child(5) { height: 55px; animation-delay: 0.05s; }
  .voice-bar:nth-child(6) { height: 35px; animation-delay: 0.25s; }
  .voice-bar:nth-child(7) { height: 45px; animation-delay: 0.1s; }
  .voice-bar:nth-child(8) { height: 30px; animation-delay: 0.3s; }
  .voice-bar:nth-child(9) { height: 20px; animation-delay: 0.05s; }
  .voice-bar.user-bar { background: var(--accent2); }
  .voice-bar.inactive { animation: none; height: 6px !important; opacity: 0.3; }

  @keyframes voiceAnim {
    0%, 100% { transform: scaleY(0.5); opacity: 0.4; }
    50% { transform: scaleY(1.2); opacity: 1; }
  }

  /* Mic Button */
  .mic-section {
    display: flex; flex-direction: column; align-items: center; gap: 16px;
    margin-top: auto;
  }

  .mic-btn {
    width: 80px; height: 80px; border-radius: 50%;
    background: var(--surface2); border: 2px solid var(--border);
    display: flex; align-items: center; justify-content: center;
    font-size: 32px; cursor: pointer; transition: all 0.3s;
    position: relative;
  }

  .mic-btn.listening {
    background: rgba(78,255,163,0.1);
    border-color: var(--accent);
    box-shadow: 0 0 0 0 rgba(78,255,163,0.4);
    animation: micPulse 1.5s infinite;
  }

  @keyframes micPulse {
    0% { box-shadow: 0 0 0 0 rgba(78,255,163,0.4); }
    70% { box-shadow: 0 0 0 20px rgba(78,255,163,0); }
    100% { box-shadow: 0 0 0 0 rgba(78,255,163,0); }
  }

  .mic-status { font-size: 13px; color: var(--text2); font-weight: 500; }

  /* Transcript */
  .transcript-area {
    background: var(--surface2); border: 1px solid var(--border);
    border-radius: 16px; padding: 20px; min-height: 100px;
    font-size: 15px; line-height: 1.7; color: var(--text2);
    margin-top: 24px;
    position: relative;
  }

  .transcript-label {
    font-size: 11px; font-weight: 700; letter-spacing: 1px;
    color: var(--text3); text-transform: uppercase; margin-bottom: 10px;
  }

  /* Sidebar elements */
  .sidebar-card {
    background: var(--surface2); border: 1px solid var(--border);
    border-radius: 16px; padding: 20px;
  }

  .sidebar-card h4 {
    font-size: 13px; font-weight: 700; color: var(--text2);
    text-transform: uppercase; letter-spacing: 1px; margin-bottom: 14px;
  }

  .question-list { display: flex; flex-direction: column; gap: 8px; }

  .q-list-item {
    display: flex; align-items: center; gap: 12px;
    padding: 10px 12px; border-radius: 10px;
    font-size: 13px; color: var(--text2); cursor: pointer;
    transition: background 0.2s;
  }

  .q-list-item:hover { background: var(--surface); }
  .q-list-item.current { background: rgba(78,255,163,0.08); color: var(--text); }

  .q-num {
    width: 24px; height: 24px; border-radius: 6px;
    background: var(--border); display: flex; align-items: center;
    justify-content: center; font-size: 11px; font-weight: 700;
    flex-shrink: 0;
  }

  .q-list-item.done .q-num { background: rgba(78,255,163,0.2); color: var(--accent); }
  .q-list-item.current .q-num { background: var(--accent); color: #000; }

  .q-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--accent3); flex-shrink: 0; }
  .q-list-item.done .q-dot { background: var(--accent); }

  /* Live metrics */
  .metric-row {
    display: flex; justify-content: space-between;
    align-items: center; padding: 8px 0;
    border-bottom: 1px solid var(--border);
  }
  .metric-row:last-child { border-bottom: none; }
  .metric-label { font-size: 13px; color: var(--text2); }
  .metric-val { font-family: 'Syne', sans-serif; font-size: 18px; font-weight: 700; }
  .metric-val.good { color: var(--accent); }
  .metric-val.warn { color: var(--accent4); }

  .progress-bar-wrap {
    background: var(--border); border-radius: 4px; height: 6px; margin-top: 4px;
  }
  .progress-bar-fill {
    height: 100%; border-radius: 4px; background: var(--accent);
    transition: width 0.5s;
  }

  /* ============ REPORT PAGE ============ */
  .report-container { max-width: 1000px; margin: 0 auto; padding: 60px 24px; }

  .report-header {
    display: flex; justify-content: space-between; align-items: flex-start;
    margin-bottom: 48px; flex-wrap: wrap; gap: 24px;
  }

  .report-score-ring {
    display: flex; flex-direction: column; align-items: center; gap: 8px;
  }

  .ring-outer {
    width: 130px; height: 130px; border-radius: 50%;
    background: conic-gradient(var(--accent) 0% 78%, var(--border) 78% 100%);
    display: flex; align-items: center; justify-content: center;
    position: relative;
  }

  .ring-inner {
    width: 100px; height: 100px; border-radius: 50%;
    background: var(--bg); display: flex; flex-direction: column;
    align-items: center; justify-content: center;
  }

  .ring-score {
    font-family: 'Syne', sans-serif; font-size: 32px; font-weight: 800;
    color: var(--text); line-height: 1;
  }

  .ring-label { font-size: 11px; color: var(--text3); font-weight: 600; letter-spacing: 1px; }

  /* Score breakdown */
  .score-grid {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 16px; margin-bottom: 48px;
  }

  .score-card {
    background: var(--surface); border: 1px solid var(--border);
    border-radius: 16px; padding: 24px; transition: all 0.2s;
  }

  .score-card:hover { border-color: rgba(78,255,163,0.2); }

  .score-card-label { font-size: 12px; color: var(--text3); font-weight: 600; letter-spacing: 1px; text-transform: uppercase; margin-bottom: 8px; }
  .score-card-val {
    font-family: 'Syne', sans-serif; font-size: 40px; font-weight: 800;
    line-height: 1; margin-bottom: 12px;
  }

  .score-card-bar { background: var(--border); border-radius: 4px; height: 4px; }
  .score-card-bar-fill { height: 100%; border-radius: 4px; transition: width 1s 0.5s; }

  /* Q&A Breakdown */
  .qa-breakdown { margin-bottom: 48px; }
  .qa-breakdown h2 { font-size: 24px; font-weight: 800; margin-bottom: 24px; }

  .qa-item {
    background: var(--surface); border: 1px solid var(--border);
    border-radius: 20px; padding: 28px; margin-bottom: 16px;
    transition: all 0.2s; cursor: pointer;
  }

  .qa-item:hover { border-color: rgba(78,255,163,0.2); }

  .qa-item-header {
    display: flex; justify-content: space-between; align-items: flex-start;
    margin-bottom: 16px; gap: 16px;
  }

  .qa-q-text { font-size: 16px; font-weight: 600; flex: 1; }
  .qa-score-badge {
    padding: 4px 14px; border-radius: 100px; font-size: 13px; font-weight: 700;
    white-space: nowrap;
  }
  .qa-score-badge.good { background: rgba(78,255,163,0.1); color: var(--accent); border: 1px solid rgba(78,255,163,0.2); }
  .qa-score-badge.avg { background: rgba(255,209,102,0.1); color: var(--accent4); border: 1px solid rgba(255,209,102,0.2); }
  .qa-score-badge.low { background: rgba(255,107,107,0.1); color: var(--accent3); border: 1px solid rgba(255,107,107,0.2); }

  .qa-answers {
    display: grid; grid-template-columns: 1fr 1fr; gap: 16px;
    margin-top: 16px;
  }

  .answer-box { border-radius: 12px; padding: 16px; font-size: 14px; line-height: 1.7; }
  .answer-box.yours { background: rgba(123,97,255,0.06); border: 1px solid rgba(123,97,255,0.2); }
  .answer-box.ideal { background: rgba(78,255,163,0.04); border: 1px solid rgba(78,255,163,0.2); }

  .answer-box-label { font-size: 11px; font-weight: 700; letter-spacing: 1px; text-transform: uppercase; margin-bottom: 8px; }
  .answer-box.yours .answer-box-label { color: var(--accent2); }
  .answer-box.ideal .answer-box-label { color: var(--accent); }
  .answer-box p { color: var(--text2); }

  /* Tips Section */
  .tips-section {
    background: var(--surface); border: 1px solid var(--border);
    border-radius: 24px; padding: 36px; margin-bottom: 48px;
  }

  .tips-section h2 { font-size: 24px; font-weight: 800; margin-bottom: 24px; }

  .tips-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 16px; }

  .tip-card {
    border-radius: 16px; padding: 20px;
    border: 1px solid transparent;
  }

  .tip-card.strength {
    background: rgba(78,255,163,0.05); border-color: rgba(78,255,163,0.15);
  }

  .tip-card.improve {
    background: rgba(255,107,107,0.05); border-color: rgba(255,107,107,0.15);
  }

  .tip-card.advice {
    background: rgba(255,209,102,0.05); border-color: rgba(255,209,102,0.15);
  }

  .tip-icon { font-size: 28px; margin-bottom: 12px; }
  .tip-card h4 { font-size: 15px; font-weight: 700; margin-bottom: 6px; }
  .tip-card p { font-size: 13px; color: var(--text2); line-height: 1.6; }

  /* ============ ARCHITECTURE PAGE ============ */
  .arch-container { max-width: 1100px; margin: 0 auto; padding: 60px 24px; }

  .arch-title { font-size: 42px; font-weight: 800; margin-bottom: 12px; letter-spacing: -1px; }
  .arch-sub { color: var(--text2); font-size: 16px; margin-bottom: 60px; }

  .arch-diagram {
    background: var(--surface); border: 1px solid var(--border);
    border-radius: 24px; padding: 48px; margin-bottom: 48px;
    overflow-x: auto;
  }

  .arch-flow {
    display: flex; flex-direction: column; gap: 0;
    min-width: 700px;
  }

  .arch-layer {
    display: flex; align-items: stretch; gap: 0;
    position: relative;
  }

  .arch-layer-label {
    width: 140px; flex-shrink: 0;
    display: flex; align-items: center;
    font-size: 11px; font-weight: 700; letter-spacing: 1px;
    text-transform: uppercase; color: var(--text3);
    padding-right: 20px;
    border-right: 1px solid var(--border);
  }

  .arch-nodes {
    flex: 1; display: flex; gap: 12px; padding: 20px 0 20px 24px;
    align-items: center; flex-wrap: wrap;
  }

  .arch-node {
    padding: 12px 18px; border-radius: 10px;
    font-size: 13px; font-weight: 600; white-space: nowrap;
    border: 1px solid; display: flex; align-items: center; gap: 8px;
  }

  .arch-node.frontend { background: rgba(123,97,255,0.1); border-color: rgba(123,97,255,0.3); color: var(--accent2); }
  .arch-node.api { background: rgba(78,255,163,0.08); border-color: rgba(78,255,163,0.25); color: var(--accent); }
  .arch-node.ai { background: rgba(255,209,102,0.08); border-color: rgba(255,209,102,0.25); color: var(--accent4); }
  .arch-node.infra { background: rgba(255,107,107,0.08); border-color: rgba(255,107,107,0.25); color: var(--accent3); }
  .arch-node.db { background: rgba(100,180,255,0.08); border-color: rgba(100,180,255,0.25); color: #64B4FF; }

  .arch-arrow {
    display: flex; justify-content: center; align-items: center;
    padding: 4px 0; color: var(--text3); font-size: 18px;
    padding-left: 164px;
  }

  /* Tech Stack */
  .stack-grid {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 20px;
  }

  .stack-card {
    background: var(--surface); border: 1px solid var(--border);
    border-radius: 16px; padding: 24px; transition: all 0.2s;
  }

  .stack-card:hover { border-color: rgba(78,255,163,0.2); transform: translateY(-2px); }

  .stack-card-header {
    display: flex; align-items: center; gap: 12px; margin-bottom: 16px;
  }

  .stack-icon {
    width: 40px; height: 40px; border-radius: 10px;
    display: flex; align-items: center; justify-content: center; font-size: 20px;
  }

  .stack-card h3 { font-size: 15px; font-weight: 700; }
  .stack-card ul { list-style: none; display: flex; flex-direction: column; gap: 6px; }
  .stack-card ul li { font-size: 13px; color: var(--text2); display: flex; align-items: center; gap: 8px; }
  .stack-card ul li::before { content: '→'; color: var(--accent); font-size: 11px; }

  /* SHARED COMPONENTS */
  .divider {
    height: 1px; background: var(--border); margin: 60px 0;
  }

  .section-center { text-align: center; }
  .section-center .section-title { margin: 0 auto 64px; }

  .badge-row { display: flex; gap: 8px; flex-wrap: wrap; margin-bottom: 12px; }
  .badge { padding: 3px 10px; border-radius: 6px; font-size: 11px; font-weight: 700; letter-spacing: 0.5px; text-transform: uppercase; }
  .badge-green { background: rgba(78,255,163,0.12); color: var(--accent); }
  .badge-purple { background: rgba(123,97,255,0.12); color: var(--accent2); }
  .badge-red { background: rgba(255,107,107,0.12); color: var(--accent3); }
  .badge-yellow { background: rgba(255,209,102,0.12); color: var(--accent4); }

  .filler-tags { display: flex; gap: 8px; flex-wrap: wrap; margin-top: 8px; }
  .filler-tag {
    background: rgba(255,107,107,0.1); border: 1px solid rgba(255,107,107,0.2);
    color: var(--accent3); padding: 4px 10px; border-radius: 100px; font-size: 12px; font-weight: 600;
  }

  /* Selected tag style for setup */
  .role-tag.selected { background: rgba(78,255,163,0.12); border-color: var(--accent); color: var(--accent); }
  .type-tag.selected { background: rgba(123,97,255,0.12); border-color: var(--accent2); color: var(--accent2); }

  /* Bottom nav on interview */
  .interview-bottom {
    display: flex; gap: 12px; margin-top: 24px; justify-content: flex-end;
  }

  .btn-icon {
    width: 44px; height: 44px; border-radius: 10px;
    background: var(--surface2); border: 1px solid var(--border);
    display: flex; align-items: center; justify-content: center;
    font-size: 18px; cursor: pointer; transition: all 0.2s;
  }
  .btn-icon:hover { border-color: var(--text2); }

  .btn-skip {
    padding: 10px 20px; border-radius: 10px;
    background: transparent; border: 1px solid var(--border);
    color: var(--text2); font-size: 14px; font-weight: 600;
    cursor: pointer; transition: all 0.2s; font-family: 'DM Sans', sans-serif;
  }
  .btn-skip:hover { border-color: var(--text2); color: var(--text); }

  .btn-next {
    padding: 10px 28px; border-radius: 10px;
    background: var(--accent); border: none;
    color: #000; font-size: 14px; font-weight: 700;
    cursor: pointer; transition: all 0.2s;
    font-family: 'Syne', sans-serif;
    display: flex; align-items: center; gap: 8px;
  }
  .btn-next:hover { box-shadow: 0 8px 24px rgba(78,255,163,0.3); }

  /* Page indicator dots */
  .nav-dots {
    display: flex; gap: 6px; position: fixed;
    bottom: 24px; left: 50%; transform: translateX(-50%);
    background: rgba(14,18,32,0.9); backdrop-filter: blur(12px);
    border: 1px solid var(--border); border-radius: 100px;
    padding: 10px 16px; z-index: 50;
  }

  .nav-dot {
    width: 8px; height: 8px; border-radius: 100px;
    background: var(--border); cursor: pointer; transition: all 0.3s;
  }
  .nav-dot.active { background: var(--accent); width: 24px; }

  /* Mobile hints */
  .ai-hint {
    background: rgba(123,97,255,0.08); border: 1px solid rgba(123,97,255,0.2);
    border-radius: 12px; padding: 14px 18px;
    display: flex; align-items: flex-start; gap: 12px;
    font-size: 13px; color: var(--text2); line-height: 1.6;
    margin-top: 20px;
  }
  .ai-hint-icon { font-size: 18px; flex-shrink: 0; margin-top: 1px; }

  /* Scrollbar */
  ::-webkit-scrollbar { width: 6px; height: 6px; }
  ::-webkit-scrollbar-track { background: transparent; }
  ::-webkit-scrollbar-thumb { background: var(--border); border-radius: 3px; }

  /* Fade-in */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .fade-in { animation: fadeUp 0.5s ease forwards; }

  /* Responsive */
  @media (max-width: 768px) {
    nav { padding: 14px 20px; }
    .nav-links { display: none; }
    .section { padding: 60px 20px; }
    .interview-layout { grid-template-columns: 1fr; }
    .interview-sidebar { display: none; }
    .qa-answers { grid-template-columns: 1fr; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="logo">
    <div class="logo-dot"></div>
    PrepSense
  </div>
  <div class="nav-links">
    <a href="#" class="active" onclick="showPage('landing')">Home</a>
    <a href="#" onclick="showPage('setup')">Start Mock</a>
    <a href="#" onclick="showPage('interview')">Interview</a>
    <a href="#" onclick="showPage('report')">Report</a>
    <a href="#" onclick="showPage('architecture')">Architecture</a>
  </div>
  <button class="nav-cta" onclick="showPage('setup')">Start Free Mock →</button>
</nav>

<!-- ===== PAGE 1: LANDING ===== -->
<div class="page active" id="page-landing">
  <div class="hero">
    <div class="hero-bg">
      <div class="hero-blob blob1"></div>
      <div class="hero-blob blob2"></div>
    </div>

    <div class="hero-badge">
      <div class="hero-badge-dot"></div>
      AI-Powered Interview Coach
    </div>

    <h1 class="hero-title">
      Ace your next<br>
      interview with<br>
      <span class="highlight">PrepSense AI</span>
    </h1>

    <p class="hero-sub">
      Upload your resume, let AI analyze your profile, and practice with a real voice interview. Get detailed feedback that actually helps you improve.
    </p>

    <div class="hero-ctas">
      <button class="btn-primary" onclick="showPage('setup')">
        🎙️ Start Free Mock Interview
      </button>
      <button class="btn-secondary" onclick="showPage('architecture')">
        View Architecture →
      </button>
    </div>

    <div class="hero-stats">
      <div class="hero-stat">
        <div class="hero-stat-num">50<span>K+</span></div>
        <div class="hero-stat-label">Mock Interviews Done</div>
      </div>
      <div class="hero-stat">
        <div class="hero-stat-num">87<span>%</span></div>
        <div class="hero-stat-label">Placement Rate</div>
      </div>
      <div class="hero-stat">
        <div class="hero-stat-num">200<span>+</span></div>
        <div class="hero-stat-label">Companies Covered</div>
      </div>
      <div class="hero-stat">
        <div class="hero-stat-num">4.9<span>★</span></div>
        <div class="hero-stat-label">Student Rating</div>
      </div>
    </div>
  </div>

  <div class="section section-center">
    <div class="section-label">Everything you need</div>
    <h2 class="section-title">Built differently, for real results</h2>

    <div class="features-grid">
      <div class="feature-card">
        <div class="feature-icon" style="background: rgba(78,255,163,0.1)">📄</div>
        <h3>Resume Intelligence</h3>
        <p>AI parses your resume and generates questions from your exact projects, skills, and experience gaps. Not generic — hyper-personalized.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon" style="background: rgba(123,97,255,0.1)">🎙️</div>
        <h3>Voice Interview Engine</h3>
        <p>Real AI voice asks questions. You speak your answers. Filler words, pace, confidence — all analyzed in real time.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon" style="background: rgba(255,209,102,0.1)">🧠</div>
        <h3>Smart Follow-ups</h3>
        <p>Give a vague answer? The AI probes deeper — just like a real interviewer. Keeps you on your toes.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon" style="background: rgba(255,107,107,0.1)">📊</div>
        <h3>Detailed Report Card</h3>
        <p>Per-question breakdown with your answer vs ideal answer side-by-side. Scores for clarity, confidence, relevance and depth.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon" style="background: rgba(78,255,163,0.1)">🎭</div>
        <h3>Interviewer Personas</h3>
        <p>Choose The Skeptic, The Friendly Senior, or Panel Mode with 3 interviewers. Practice for any style.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon" style="background: rgba(123,97,255,0.1)">🏫</div>
        <h3>College TPO Dashboard</h3>
        <p>For placement officers — track entire batch readiness, set company-specific prep packs, and export placement reports.</p>
      </div>
    </div>
  </div>
</div>

<!-- ===== PAGE 2: SETUP ===== -->
<div class="page" id="page-setup">
  <div class="setup-container">
    <div class="setup-header">
      <h1 style="font-size: 36px; font-weight: 800; letter-spacing: -1px; margin-bottom: 8px;">Set up your mock interview</h1>
      <p style="color: var(--text2); font-size: 16px;">Takes 2 minutes. The more detail you give, the better the questions.</p>
    </div>

    <div class="setup-step-indicator">
      <div class="step-item done" id="step1">
        <div class="step-circle">✓</div>
        <div class="step-label">Resume</div>
      </div>
      <div class="step-line"></div>
      <div class="step-item active" id="step2">
        <div class="step-circle">2</div>
        <div class="step-label">Role & Preferences</div>
      </div>
      <div class="step-line"></div>
      <div class="step-item" id="step3">
        <div class="step-circle">3</div>
        <div class="step-label">Interview Style</div>
      </div>
      <div class="step-line"></div>
      <div class="step-item" id="step4">
        <div class="step-circle">4</div>
        <div class="step-label">Go Live</div>
      </div>
    </div>

    <!-- Upload -->
    <div class="upload-zone" id="uploadZone">
      <div class="upload-icon">📄</div>
      <h3>Drop your resume here</h3>
      <p style="color: var(--text2)">or click to browse files</p>
      <p class="upload-hint">PDF, DOCX, or TXT · Max 5MB</p>
    </div>

    <div class="ai-hint" style="margin-top: 16px; margin-bottom: 32px;">
      <span class="ai-hint-icon">✨</span>
      <span>AI will extract your skills, projects, experience gaps and prepare targeted questions — including asking about <strong>specific projects</strong> you've worked on.</span>
    </div>

    <!-- OR manually add -->
    <div style="display: flex; align-items: center; gap: 16px; margin-bottom: 28px;">
      <div style="flex: 1; height: 1px; background: var(--border);"></div>
      <span style="color: var(--text3); font-size: 13px; font-weight: 600;">OR ADD MANUALLY</span>
      <div style="flex: 1; height: 1px; background: var(--border);"></div>
    </div>

    <div class="form-group">
      <label class="form-label">Your Key Skills & Experience</label>
      <textarea class="form-textarea" placeholder="e.g. 3 years React, Node.js. Built a payment gateway at XYZ startup. Led team of 4. Strong in DSA..."></textarea>
    </div>

    <div class="form-group">
      <label class="form-label">Job Description (paste JD for hyper-targeted questions)</label>
      <textarea class="form-textarea" placeholder="Paste the JD here. AI will identify gaps between your profile and this role..."></textarea>
    </div>

    <div class="form-group">
      <label class="form-label">Target Role</label>
      <div class="tag-selector" id="roleSelector">
        <div class="tag role-tag selected" onclick="toggleTag(this, 'role')">Software Engineer</div>
        <div class="tag role-tag" onclick="toggleTag(this, 'role')">Data Analyst</div>
        <div class="tag role-tag" onclick="toggleTag(this, 'role')">Product Manager</div>
        <div class="tag role-tag" onclick="toggleTag(this, 'role')">DevOps Engineer</div>
        <div class="tag role-tag" onclick="toggleTag(this, 'role')">Data Scientist</div>
        <div class="tag role-tag" onclick="toggleTag(this, 'role')">Marketing</div>
        <div class="tag role-tag" onclick="toggleTag(this, 'role')">Finance / CA</div>
        <div class="tag role-tag" onclick="toggleTag(this, 'role')">Other</div>
      </div>
    </div>

    <div class="form-group">
      <label class="form-label">Experience Level</label>
      <div class="tag-selector">
        <div class="tag type-tag selected" onclick="toggleTag(this, 'exp')">Fresher</div>
        <div class="tag type-tag" onclick="toggleTag(this, 'exp')">1–3 Years</div>
        <div class="tag type-tag" onclick="toggleTag(this, 'exp')">3–6 Years</div>
        <div class="tag type-tag" onclick="toggleTag(this, 'exp')">6+ Years</div>
      </div>
    </div>

    <div class="form-group">
      <label class="form-label">Interview Type</label>
      <div class="tag-selector">
        <div class="tag role-tag selected" onclick="toggleTag(this, 'type')">Mixed (HR + Tech)</div>
        <div class="tag role-tag" onclick="toggleTag(this, 'type')">Technical Only</div>
        <div class="tag role-tag" onclick="toggleTag(this, 'type')">HR / Behavioral</div>
        <div class="tag role-tag" onclick="toggleTag(this, 'type')">System Design</div>
        <div class="tag role-tag" onclick="toggleTag(this, 'type')">Case Study</div>
      </div>
    </div>

    <div class="form-group">
      <label class="form-label">Difficulty</label>
      <div class="difficulty-track">
        <div class="diff-btn easy selected" onclick="setDiff(this)">😌 Easy</div>
        <div class="diff-btn medium" onclick="setDiff(this)">🔥 Medium</div>
        <div class="diff-btn hard" onclick="setDiff(this)">💀 Brutal</div>
      </div>
    </div>

    <div class="form-group">
      <label class="form-label">Interviewer Persona</label>
      <div class="tag-selector">
        <div class="tag type-tag selected" onclick="toggleTag(this, 'persona')">🤝 Friendly Senior</div>
        <div class="tag type-tag" onclick="toggleTag(this, 'persona')">🔍 The Skeptic</div>
        <div class="tag type-tag" onclick="toggleTag(this, 'persona')">😐 The Silent Type</div>
        <div class="tag type-tag" onclick="toggleTag(this, 'persona')">👥 Panel (3 people)</div>
        <div class="tag type-tag" onclick="toggleTag(this, 'persona')">⚡ Speed Round</div>
      </div>
    </div>

    <div class="setup-actions">
      <div style="color: var(--text3); font-size: 13px;">~10 questions · ~30 min</div>
      <button class="btn-primary" onclick="showPage('interview')">
        Generate Questions & Start →
      </button>
    </div>
  </div>
</div>

<!-- ===== PAGE 3: INTERVIEW ===== -->
<div class="page" id="page-interview">
  <div class="interview-layout">

    <!-- MAIN AREA -->
    <div class="interview-main">

      <div class="question-counter">
        <div class="q-badge">Question 3 of 10</div>
        <div style="display: flex; align-items: center; gap: 16px;">
          <div class="btn-icon" title="Pause">⏸</div>
          <div class="q-timer">
            <div class="q-timer-circle">42</div>
          </div>
        </div>
      </div>

      <!-- Progress -->
      <div style="margin-bottom: 32px;">
        <div style="display: flex; justify-content: space-between; font-size: 12px; color: var(--text3); margin-bottom: 8px;">
          <span>Progress</span><span>30%</span>
        </div>
        <div class="progress-bar-wrap">
          <div class="progress-bar-fill" style="width: 30%; background: linear-gradient(90deg, var(--accent2), var(--accent));"></div>
        </div>
      </div>

      <!-- AI Voice Visualizer -->
      <div style="text-align: center; margin-bottom: 24px;">
        <div style="font-size: 12px; color: var(--text3); letter-spacing: 1px; text-transform: uppercase; margin-bottom: 12px; font-weight: 600;">🤖 AI Interviewer Speaking</div>
        <div class="voice-visualizer">
          <div class="voice-bar"></div>
          <div class="voice-bar"></div>
          <div class="voice-bar"></div>
          <div class="voice-bar"></div>
          <div class="voice-bar"></div>
          <div class="voice-bar"></div>
          <div class="voice-bar"></div>
          <div class="voice-bar"></div>
          <div class="voice-bar"></div>
        </div>
      </div>

      <!-- Question Box -->
      <div class="question-box">
        <div class="question-type-tag">Behavioral</div>
        <div class="question-text">
          You mentioned building a payment gateway at XYZ Startup in your resume. Walk me through a specific technical failure you encountered and exactly how you resolved it.
        </div>

        <div class="ai-hint" style="margin-top: 28px;">
          <span class="ai-hint-icon">💡</span>
          <span>Try using the <strong>STAR format</strong> — Situation, Task, Action, Result. Include specific metrics if you can.</span>
        </div>
      </div>

      <!-- User Transcript -->
      <div class="transcript-area" style="margin-top: 24px;">
        <div class="transcript-label">📝 Your Answer (Live Transcript)</div>
        <p style="color: var(--text2);">So when we were building the payment integration... we had this issue where... um... the webhook events were coming in out of order and it was causing duplicate charges for some users. What I did was, I implemented an idempotency key system where each transaction had a unique identifier...</p>
        <div class="filler-tags">
          <div class="filler-tag">⚠ "um" detected</div>
          <div class="filler-tag">⚠ Slow pace</div>
        </div>
      </div>

      <!-- Mic Section -->
      <div class="mic-section" style="margin-top: 32px;">
        <div class="mic-btn listening" id="micBtn" onclick="toggleMic()">🎙️</div>
        <div class="mic-status" id="micStatus">Listening... speak naturally</div>
      </div>

      <div class="interview-bottom">
        <button class="btn-skip">Skip Question</button>
        <button class="btn-next" onclick="showPage('report')">Submit & Next →</button>
      </div>
    </div>

    <!-- SIDEBAR -->
    <div class="interview-sidebar">

      <!-- Live Metrics -->
      <div class="sidebar-card">
        <h4>Live Metrics</h4>
        <div class="metric-row">
          <span class="metric-label">Clarity</span>
          <span class="metric-val good">7.2</span>
        </div>
        <div class="progress-bar-wrap" style="margin-bottom: 12px;">
          <div class="progress-bar-fill" style="width: 72%;"></div>
        </div>
        <div class="metric-row">
          <span class="metric-label">Confidence</span>
          <span class="metric-val warn">5.8</span>
        </div>
        <div class="progress-bar-wrap" style="margin-bottom: 12px;">
          <div class="progress-bar-fill" style="width: 58%; background: var(--accent4);"></div>
        </div>
        <div class="metric-row">
          <span class="metric-label">Pace (WPM)</span>
          <span class="metric-val warn">105</span>
        </div>
        <div class="metric-row">
          <span class="metric-label">Filler Words</span>
          <span class="metric-val" style="color: var(--accent3);">3</span>
        </div>
        <div class="metric-row">
          <span class="metric-label">Answer Length</span>
          <span class="metric-val good">Good</span>
        </div>
      </div>

      <!-- Question List -->
      <div class="sidebar-card">
        <h4>Questions</h4>
        <div class="question-list">
          <div class="q-list-item done">
            <div class="q-num">1</div>
            <div style="flex: 1; font-size: 12px;">Tell me about yourself</div>
            <div class="q-dot"></div>
          </div>
          <div class="q-list-item done">
            <div class="q-num">2</div>
            <div style="flex: 1; font-size: 12px;">Why this company?</div>
            <div class="q-dot"></div>
          </div>
          <div class="q-list-item current">
            <div class="q-num">3</div>
            <div style="flex: 1; font-size: 12px;">Payment gateway failure</div>
            <div style="width: 6px; height: 6px; border-radius: 50%; background: var(--accent); animation: pulse 1.5s infinite; flex-shrink: 0;"></div>
          </div>
          <div class="q-list-item">
            <div class="q-num">4</div>
            <div style="flex: 1; font-size: 12px;">System design: URL shortener</div>
            <div class="q-dot" style="background: var(--border);"></div>
          </div>
          <div class="q-list-item">
            <div class="q-num">5</div>
            <div style="flex: 1; font-size: 12px;">Biggest weakness?</div>
            <div class="q-dot" style="background: var(--border);"></div>
          </div>
          <div class="q-list-item" style="color: var(--text3);">
            <div class="q-num">+5</div>
            <div style="flex: 1; font-size: 12px; color: var(--text3);">more questions...</div>
          </div>
        </div>
      </div>

      <!-- Session Stats -->
      <div class="sidebar-card">
        <h4>Session</h4>
        <div class="metric-row">
          <span class="metric-label">Time Elapsed</span>
          <span class="metric-val" style="font-size: 15px;">12:34</span>
        </div>
        <div class="metric-row">
          <span class="metric-label">Avg Score</span>
          <span class="metric-val good" style="font-size: 15px;">6.9</span>
        </div>
        <div class="metric-row">
          <span class="metric-label">Total Umms</span>
          <span class="metric-val" style="color: var(--accent3); font-size: 15px;">7</span>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- ===== PAGE 4: REPORT ===== -->
<div class="page" id="page-report">
  <div class="report-container">

    <div class="report-header">
      <div>
        <div style="font-size: 13px; color: var(--text3); font-weight: 600; letter-spacing: 1px; text-transform: uppercase; margin-bottom: 8px;">Interview Complete</div>
        <h1 style="font-size: 40px; font-weight: 800; letter-spacing: -1px; margin-bottom: 8px;">Your Report Card</h1>
        <p style="color: var(--text2);">Software Engineer · Mixed · 10 Questions · 28 min</p>
        <div class="badge-row" style="margin-top: 16px;">
          <span class="badge badge-green">Friendly Senior Mode</span>
          <span class="badge badge-purple">Medium Difficulty</span>
          <span class="badge badge-yellow">Fresher</span>
        </div>
      </div>
      <div style="display: flex; align-items: center; gap: 32px;">
        <div class="report-score-ring">
          <div class="ring-outer">
            <div class="ring-inner">
              <div class="ring-score">78</div>
              <div class="ring-label">/100</div>
            </div>
          </div>
          <div style="font-size: 13px; color: var(--text2); font-weight: 600;">Overall Score</div>
        </div>
        <div style="text-align: right;">
          <button class="btn-primary" style="margin-bottom: 10px; font-size: 14px; padding: 12px 20px;">⬇ Download PDF Report</button>
          <div style="font-size: 12px; color: var(--text3); cursor: pointer;" onclick="showPage('setup')">🔄 Try Again</div>
        </div>
      </div>
    </div>

    <!-- Score Cards -->
    <div class="score-grid">
      <div class="score-card">
        <div class="score-card-label">Clarity</div>
        <div class="score-card-val" style="color: var(--accent);">8.1</div>
        <div class="score-card-bar">
          <div class="score-card-bar-fill" style="width: 81%; background: var(--accent);"></div>
        </div>
      </div>
      <div class="score-card">
        <div class="score-card-label">Confidence</div>
        <div class="score-card-val" style="color: var(--accent4);">6.4</div>
        <div class="score-card-bar">
          <div class="score-card-bar-fill" style="width: 64%; background: var(--accent4);"></div>
        </div>
      </div>
      <div class="score-card">
        <div class="score-card-label">Relevance</div>
        <div class="score-card-val" style="color: var(--accent);">8.8</div>
        <div class="score-card-bar">
          <div class="score-card-bar-fill" style="width: 88%; background: var(--accent);"></div>
        </div>
      </div>
      <div class="score-card">
        <div class="score-card-label">Depth</div>
        <div class="score-card-val" style="color: var(--accent3);">5.9</div>
        <div class="score-card-bar">
          <div class="score-card-bar-fill" style="width: 59%; background: var(--accent3);"></div>
        </div>
      </div>
    </div>

    <!-- Voice Analysis -->
    <div class="sidebar-card" style="margin-bottom: 32px; display: flex; gap: 24px; flex-wrap: wrap;">
      <div>
        <div style="font-size: 11px; color: var(--text3); font-weight: 700; letter-spacing: 1px; text-transform: uppercase; margin-bottom: 6px;">Total "Umm/Uh" Used</div>
        <div style="font-family: 'Syne', sans-serif; font-size: 36px; font-weight: 800; color: var(--accent3);">14 <span style="font-size: 14px; color: var(--text3);">times</span></div>
      </div>
      <div>
        <div style="font-size: 11px; color: var(--text3); font-weight: 700; letter-spacing: 1px; text-transform: uppercase; margin-bottom: 6px;">Avg Speaking Pace</div>
        <div style="font-family: 'Syne', sans-serif; font-size: 36px; font-weight: 800; color: var(--accent4);">118 <span style="font-size: 14px; color: var(--text3);">WPM</span></div>
      </div>
      <div>
        <div style="font-size: 11px; color: var(--text3); font-weight: 700; letter-spacing: 1px; text-transform: uppercase; margin-bottom: 6px;">Longest Silence</div>
        <div style="font-family: 'Syne', sans-serif; font-size: 36px; font-weight: 800; color: var(--accent3);">8.2s <span style="font-size: 14px; color: var(--text3);">Q7</span></div>
      </div>
      <div>
        <div style="font-size: 11px; color: var(--text3); font-weight: 700; letter-spacing: 1px; text-transform: uppercase; margin-bottom: 6px;">Best Question</div>
        <div style="font-family: 'Syne', sans-serif; font-size: 36px; font-weight: 800; color: var(--accent);">Q4 <span style="font-size: 14px; color: var(--text3);">9.1/10</span></div>
      </div>
    </div>

    <!-- Q&A Breakdown -->
    <div class="qa-breakdown">
      <h2>Question-by-Question Breakdown</h2>

      <div class="qa-item">
        <div class="qa-item-header">
          <div>
            <div style="font-size: 11px; color: var(--text3); text-transform: uppercase; letter-spacing: 1px; font-weight: 700; margin-bottom: 6px;">Q3 · Behavioral</div>
            <div class="qa-q-text">You built a payment gateway — describe a specific technical failure and how you resolved it.</div>
          </div>
          <div class="qa-score-badge avg">6.8 / 10</div>
        </div>

        <div class="qa-answers">
          <div class="answer-box yours">
            <div class="answer-box-label">Your Answer</div>
            <p>So we had this issue where webhook events were coming out of order causing duplicate charges. I implemented idempotency keys... um... it took about 2 weeks to fully fix.</p>
          </div>
          <div class="answer-box ideal">
            <div class="answer-box-label">Ideal Answer Structure</div>
            <p><strong>Situation:</strong> "We processed 10K+ daily transactions when we noticed a 0.3% duplicate charge rate..." <strong>Action:</strong> "I designed an idempotency layer using Redis with TTL..." <strong>Result:</strong> "Reduced duplicate charges to zero within 48 hours."</p>
          </div>
        </div>

        <div style="margin-top: 16px; padding: 14px 16px; background: rgba(255,209,102,0.06); border: 1px solid rgba(255,209,102,0.15); border-radius: 10px; font-size: 13px; color: var(--text2); line-height: 1.6;">
          <strong style="color: var(--accent4);">💡 What to improve:</strong> Good technical content but missing quantification. Add specific numbers (error rate, users affected, time to fix). Lead with the impact first, then explain the solution.
        </div>
      </div>

      <div class="qa-item">
        <div class="qa-item-header">
          <div>
            <div style="font-size: 11px; color: var(--text3); text-transform: uppercase; letter-spacing: 1px; font-weight: 700; margin-bottom: 6px;">Q4 · Technical</div>
            <div class="qa-q-text">Design a URL shortener like bit.ly. Walk through your approach.</div>
          </div>
          <div class="qa-score-badge good">9.1 / 10</div>
        </div>
        <div style="padding: 14px 16px; background: rgba(78,255,163,0.05); border: 1px solid rgba(78,255,163,0.15); border-radius: 10px; font-size: 13px; color: var(--text2); line-height: 1.6;">
          <strong style="color: var(--accent);">✅ Excellent:</strong> You covered scalability, hash collision handling, database choice rationale, and caching. Structured answer with clear layers. This is exactly what interviewers want.
        </div>
      </div>

      <div class="qa-item">
        <div class="qa-item-header">
          <div>
            <div style="font-size: 11px; color: var(--text3); text-transform: uppercase; letter-spacing: 1px; font-weight: 700; margin-bottom: 6px;">Q7 · HR</div>
            <div class="qa-q-text">What's your biggest weakness?</div>
          </div>
          <div class="qa-score-badge low">4.2 / 10</div>
        </div>

        <div class="qa-answers">
          <div class="answer-box yours">
            <div class="answer-box-label">Your Answer</div>
            <p>Um... I think... I sometimes work too hard? I'm kind of a perfectionist... [8 second pause] ...yeah, I guess that's my weakness.</p>
          </div>
          <div class="answer-box ideal">
            <div class="answer-box-label">Ideal Answer Structure</div>
            <p><strong>Real weakness:</strong> "I used to struggle with delegating — I'd rewrite teammates' code..." <strong>What you did:</strong> "I started doing code reviews instead..." <strong>Growth:</strong> "Now my team's velocity improved 30%."</p>
          </div>
        </div>

        <div style="margin-top: 16px; padding: 14px 16px; background: rgba(255,107,107,0.05); border: 1px solid rgba(255,107,107,0.15); border-radius: 10px; font-size: 13px; color: var(--text2); line-height: 1.6;">
          <strong style="color: var(--accent3);">❌ Needs work:</strong> "Perfectionist" is the most overused interview answer and raises red flags. The 8-second pause showed nerves. Pick a real, minor weakness and show how you actively fixed it.
        </div>
      </div>
    </div>

    <!-- Tips Section -->
    <div class="tips-section">
      <h2>Personal Coaching Tips</h2>
      <div class="tips-grid">
        <div class="tip-card strength">
          <div class="tip-icon">💪</div>
          <h4>Your Strengths</h4>
          <p>Technical depth is excellent. You explain complex concepts clearly and you structured your system design answer perfectly.</p>
        </div>
        <div class="tip-card improve">
          <div class="tip-icon">🎯</div>
          <h4>Top Priority to Fix</h4>
          <p>Reduce filler words (14 in one session). Record yourself for 2 minutes daily — awareness alone reduces them 60%.</p>
        </div>
        <div class="tip-card advice">
          <div class="tip-icon">🧘</div>
          <h4>On Confidence</h4>
          <p>When you don't know something, say: "That's a great question — let me think through this." Pausing confidently is better than rushing nervously.</p>
        </div>
        <div class="tip-card advice">
          <div class="tip-icon">📏</div>
          <h4>Answer Length</h4>
          <p>Most of your answers were the right length. For behavioral questions, aim for 90–120 seconds. For technical, go longer — show your thinking.</p>
        </div>
        <div class="tip-card improve">
          <div class="tip-icon">🔢</div>
          <h4>Add Numbers Everywhere</h4>
          <p>"Improved performance" is weak. "Reduced API response time by 40%" is memorable. Go back through your resume and quantify every bullet.</p>
        </div>
        <div class="tip-card strength">
          <div class="tip-icon">📈</div>
          <h4>Progress vs Last Session</h4>
          <p>Your overall score improved from 61 → 78 in 3 sessions. Technical score up 2.1 points. Keep going — you're on a great trajectory.</p>
        </div>
      </div>
    </div>

    <!-- Actions -->
    <div style="display: flex; gap: 16px; flex-wrap: wrap;">
      <button class="btn-primary" onclick="showPage('setup')">🔄 Retry Weak Questions</button>
      <button class="btn-secondary">📊 View Progress Dashboard</button>
      <button class="btn-secondary">📤 Share Score Card</button>
    </div>

  </div>
</div>

<!-- ===== PAGE 5: ARCHITECTURE ===== -->
<div class="page" id="page-architecture">
  <div class="arch-container">

    <div class="arch-title">System Architecture</div>
    <p class="arch-sub">Full platform architecture for PrepSense — from resume upload to AI interview to detailed report generation.</p>

    <!-- Architecture Diagram -->
    <div class="arch-diagram">
      <h3 style="font-size: 16px; font-weight: 700; margin-bottom: 32px; color: var(--text2);">PLATFORM FLOW DIAGRAM</h3>
      <div class="arch-flow">

        <div class="arch-layer">
          <div class="arch-layer-label">Frontend</div>
          <div class="arch-nodes">
            <div class="arch-node frontend">⚛ React App</div>
            <div class="arch-node frontend">🎙 Web Speech API</div>
            <div class="arch-node frontend">🔊 TTS (ElevenLabs)</div>
            <div class="arch-node frontend">📱 Mobile PWA</div>
          </div>
        </div>

        <div class="arch-arrow">↕ REST + WebSocket</div>

        <div class="arch-layer">
          <div class="arch-layer-label">API Layer</div>
          <div class="arch-nodes">
            <div class="arch-node api">🚀 FastAPI / Node.js</div>
            <div class="arch-node api">🔐 Auth (Clerk/JWT)</div>
            <div class="arch-node api">📁 File Upload Handler</div>
            <div class="arch-node api">⚡ WebSocket Server</div>
          </div>
        </div>

        <div class="arch-arrow">↕</div>

        <div class="arch-layer">
          <div class="arch-layer-label">AI Engine</div>
          <div class="arch-nodes">
            <div class="arch-node ai">🧠 Claude API (Questions)</div>
            <div class="arch-node ai">📄 Resume Parser (LLM)</div>
            <div class="arch-node ai">🎙 Whisper (STT)</div>
            <div class="arch-node ai">📊 Report Generator</div>
            <div class="arch-node ai">🔍 JD Gap Analyzer</div>
          </div>
        </div>

        <div class="arch-arrow">↕</div>

        <div class="arch-layer">
          <div class="arch-layer-label">Database</div>
          <div class="arch-nodes">
            <div class="arch-node db">🗄 PostgreSQL (Users)</div>
            <div class="arch-node db">📦 Redis (Sessions)</div>
            <div class="arch-node db">🗂 S3 (Resumes)</div>
            <div class="arch-node db">📈 TimeSeries (Analytics)</div>
          </div>
        </div>

        <div class="arch-arrow">↕</div>

        <div class="arch-layer">
          <div class="arch-layer-label">Infra</div>
          <div class="arch-nodes">
            <div class="arch-node infra">☁ AWS / Vercel</div>
            <div class="arch-node infra">🔄 CI/CD Pipeline</div>
            <div class="arch-node infra">📊 Analytics (Mixpanel)</div>
            <div class="arch-node infra">🛡 Rate Limiting</div>
          </div>
        </div>
      </div>
    </div>

    <!-- Tech Stack -->
    <h2 style="font-size: 28px; font-weight: 800; margin-bottom: 28px; letter-spacing: -0.5px;">Tech Stack Breakdown</h2>
    <div class="stack-grid">
      <div class="stack-card">
        <div class="stack-card-header">
          <div class="stack-icon" style="background: rgba(123,97,255,0.1);">⚛</div>
          <h3>Frontend</h3>
        </div>
        <ul>
          <li>React + TypeScript</li>
          <li>Tailwind CSS</li>
          <li>Web Speech API (STT)</li>
          <li>Framer Motion (animations)</li>
          <li>Zustand (state management)</li>
        </ul>
      </div>
      <div class="stack-card">
        <div class="stack-card-header">
          <div class="stack-icon" style="background: rgba(78,255,163,0.1);">🚀</div>
          <h3>Backend</h3>
        </div>
        <ul>
          <li>FastAPI (Python) or Node.js</li>
          <li>WebSocket for real-time</li>
          <li>Celery (async tasks)</li>
          <li>PDF parser (PyMuPDF)</li>
          <li>JWT Authentication</li>
        </ul>
      </div>
      <div class="stack-card">
        <div class="stack-card-header">
          <div class="stack-icon" style="background: rgba(255,209,102,0.1);">🧠</div>
          <h3>AI / ML</h3>
        </div>
        <ul>
          <li>Claude API (questions + report)</li>
          <li>Whisper API (speech-to-text)</li>
          <li>ElevenLabs (text-to-speech)</li>
          <li>Sentence transformers</li>
          <li>Sentiment analysis model</li>
        </ul>
      </div>
      <div class="stack-card">
        <div class="stack-card-header">
          <div class="stack-icon" style="background: rgba(100,180,255,0.1);">🗄</div>
          <h3>Database & Storage</h3>
        </div>
        <ul>
          <li>PostgreSQL (user data)</li>
          <li>Redis (session + cache)</li>
          <li>AWS S3 (resume files)</li>
          <li>Pinecone (vector search)</li>
        </ul>
      </div>
      <div class="stack-card">
        <div class="stack-card-header">
          <div class="stack-icon" style="background: rgba(255,107,107,0.1);">☁</div>
          <h3>DevOps</h3>
        </div>
        <ul>
          <li>Vercel (frontend deploy)</li>
          <li>Railway / Render (backend)</li>
          <li>GitHub Actions (CI/CD)</li>
          <li>Docker containers</li>
        </ul>
      </div>
      <div class="stack-card">
        <div class="stack-card-header">
          <div class="stack-icon" style="background: rgba(78,255,163,0.1);">🔑</div>
          <h3>3rd Party APIs</h3>
        </div>
        <ul>
          <li>Anthropic Claude (core AI)</li>
          <li>ElevenLabs (voice)</li>
          <li>OpenAI Whisper (STT)</li>
          <li>Clerk (auth)</li>
          <li>Stripe (payments)</li>
        </ul>
      </div>
    </div>

    <!-- MVP Build Plan -->
    <div style="margin-top: 60px; background: var(--surface); border: 1px solid var(--border); border-radius: 24px; padding: 40px;">
      <h2 style="font-size: 24px; font-weight: 800; margin-bottom: 8px;">Hackathon MVP Build Plan</h2>
      <p style="color: var(--text2); font-size: 14px; margin-bottom: 32px;">What to actually build vs what to mockup in your slides</p>

      <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 24px;">
        <div>
          <div style="font-size: 12px; font-weight: 700; color: var(--accent); letter-spacing: 1px; text-transform: uppercase; margin-bottom: 16px; display: flex; align-items: center; gap: 8px;">
            <span>✅</span> ACTUALLY BUILD
          </div>
          <div style="display: flex; flex-direction: column; gap: 10px;">
            <div style="padding: 12px 16px; background: rgba(78,255,163,0.05); border: 1px solid rgba(78,255,163,0.15); border-radius: 10px; font-size: 14px; color: var(--text2);">Resume upload → AI question generation</div>
            <div style="padding: 12px 16px; background: rgba(78,255,163,0.05); border: 1px solid rgba(78,255,163,0.15); border-radius: 10px; font-size: 14px; color: var(--text2);">Voice interview (TTS + STT)</div>
            <div style="padding: 12px 16px; background: rgba(78,255,163,0.05); border: 1px solid rgba(78,255,163,0.15); border-radius: 10px; font-size: 14px; color: var(--text2);">AI follow-up on vague answers</div>
            <div style="padding: 12px 16px; background: rgba(78,255,163,0.05); border: 1px solid rgba(78,255,163,0.15); border-radius: 10px; font-size: 14px; color: var(--text2);">Post-interview report with ideal answers</div>
            <div style="padding: 12px 16px; background: rgba(78,255,163,0.05); border: 1px solid rgba(78,255,163,0.15); border-radius: 10px; font-size: 14px; color: var(--text2);">Filler word counter</div>
          </div>
        </div>
        <div>
          <div style="font-size: 12px; font-weight: 700; color: var(--accent4); letter-spacing: 1px; text-transform: uppercase; margin-bottom: 16px; display: flex; align-items: center; gap: 8px;">
            <span>🖼</span> SHOW AS MOCKUP
          </div>
          <div style="display: flex; flex-direction: column; gap: 10px;">
            <div style="padding: 12px 16px; background: rgba(255,209,102,0.05); border: 1px solid rgba(255,209,102,0.15); border-radius: 10px; font-size: 14px; color: var(--text2);">College TPO Dashboard</div>
            <div style="padding: 12px 16px; background: rgba(255,209,102,0.05); border: 1px solid rgba(255,209,102,0.15); border-radius: 10px; font-size: 14px; color: var(--text2);">Video emotion analysis</div>
            <div style="padding: 12px 16px; background: rgba(255,209,102,0.05); border: 1px solid rgba(255,209,102,0.15); border-radius: 10px; font-size: 14px; color: var(--text2);">Peer matchmaking</div>
            <div style="padding: 12px 16px; background: rgba(255,209,102,0.05); border: 1px solid rgba(255,209,102,0.15); border-radius: 10px; font-size: 14px; color: var(--text2);">Progress analytics dashboard</div>
            <div style="padding: 12px 16px; background: rgba(255,209,102,0.05); border: 1px solid rgba(255,209,102,0.15); border-radius: 10px; font-size: 14px; color: var(--text2);">Company-specific prep packs</div>
          </div>
        </div>
      </div>
    </div>

  </div>
</div>

<!-- PAGE NAV DOTS -->
<div class="nav-dots">
  <div class="nav-dot active" onclick="showPage('landing')" title="Home"></div>
  <div class="nav-dot" onclick="showPage('setup')" title="Setup"></div>
  <div class="nav-dot" onclick="showPage('interview')" title="Interview"></div>
  <div class="nav-dot" onclick="showPage('report')" title="Report"></div>
  <div class="nav-dot" onclick="showPage('architecture')" title="Architecture"></div>
</div>

<script>
  const pages = ['landing', 'setup', 'interview', 'report', 'architecture'];

  function showPage(name) {
    pages.forEach(p => {
      document.getElementById('page-' + p).classList.remove('active');
    });
    document.getElementById('page-' + name).classList.add('active');

    // Update nav dots
    document.querySelectorAll('.nav-dot').forEach((dot, i) => {
      dot.classList.toggle('active', pages[i] === name);
    });

    window.scrollTo(0, 0);
  }

  // Upload zone
  const zone = document.getElementById('uploadZone');
  if (zone) {
    zone.addEventListener('dragover', e => { e.preventDefault(); zone.classList.add('dragover'); });
    zone.addEventListener('dragleave', () => zone.classList.remove('dragover'));
    zone.addEventListener('drop', e => {
      e.preventDefault(); zone.classList.remove('dragover');
      zone.innerHTML = '<div class="upload-icon">✅</div><h3>resume_john_doe.pdf</h3><p style="color: var(--accent)">Parsed successfully · 12 skills detected · 3 projects found</p>';
    });
    zone.addEventListener('click', () => {
      zone.innerHTML = '<div class="upload-icon">✅</div><h3>resume_john_doe.pdf</h3><p style="color: var(--accent)">Parsed successfully · 12 skills detected · 3 projects found</p>';
    });
  }

  // Tag selectors
  function toggleTag(el, group) {
    const parent = el.parentElement;
    parent.querySelectorAll('.tag').forEach(t => {
      if (t.classList.contains('role-tag') && el.classList.contains('role-tag')) t.classList.remove('selected');
      if (t.classList.contains('type-tag') && el.classList.contains('type-tag')) t.classList.remove('selected');
    });
    el.classList.add('selected');
  }

  // Difficulty
  function setDiff(el) {
    document.querySelectorAll('.diff-btn').forEach(d => d.classList.remove('selected'));
    el.classList.add('selected');
  }

  // Mic toggle
  let isListening = false;
  function toggleMic() {
    isListening = !isListening;
    const btn = document.getElementById('micBtn');
    const status = document.getElementById('micStatus');
    if (isListening) {
      btn.classList.add('listening');
      status.textContent = 'Listening... speak naturally';
      status.style.color = 'var(--accent)';
    } else {
      btn.classList.remove('listening');
      status.textContent = 'Click mic to start answering';
      status.style.color = 'var(--text2)';
    }
  }

  // Timer animation
  let time = 42;
  setInterval(() => {
    const el = document.querySelector('.q-timer-circle');
    if (el && document.getElementById('page-interview').classList.contains('active')) {
      time = Math.max(0, time - 1);
      el.textContent = time;
      if (time < 15) el.style.borderColor = 'var(--accent3)';
      else if (time < 30) el.style.borderColor = 'var(--accent4)';
      else el.style.borderColor = 'var(--accent)';
      if (time === 0) time = 90;
    }
  }, 1000);
</script>

</body>
</html>